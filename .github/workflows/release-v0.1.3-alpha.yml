name: release-v0.1.3-alpha (retired draft)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: windows-2022
    steps:
      - shell: pwsh
        run: |
          Write-Host 'This malformed draft is retired. Use release-v0.1.3-alpha-clean.yml.'
