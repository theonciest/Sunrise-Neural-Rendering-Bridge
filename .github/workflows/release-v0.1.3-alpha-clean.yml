name: release-v0.1.3-alpha-clean-room (retired draft)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: windows-2022
    steps:
      - shell: pwsh
        run: |
          Write-Host 'Retired after provenance check. Use release-v0.1.3-alpha-sourcehost.yml.'
